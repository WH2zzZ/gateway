spring:
  application:
    name: gateway
  profiles:
    active: default
  cloud:
    config:
      #Config server的uri
      uri: http://127.0.0.1:9000/
      #指定的环境
      profile: dev
      #指定分支
      label: master

