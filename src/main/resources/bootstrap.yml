spring:
  application:
    name: gateway
  profiles:
    active: default
  cloud:
    config:
      #Config server的uri
      uri: http://configurationserver_1:9000/
      #指定的环境
      profile: default
      #指定分支
      label: master

